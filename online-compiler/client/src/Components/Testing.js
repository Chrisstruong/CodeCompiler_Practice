const Testing = (props) => {
    // let code = props.code
    // code += `mostFrequentChar('bookeeper')`
    // console.log(code)
    return(
        <>
        
        </>
    )
}

export default Testing